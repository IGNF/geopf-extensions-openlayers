/**
 * Typedefs partagés pour le contrôle Draw.
 * Ce fichier centralise les types réutilisables afin que typedoc génère des docs cohérentes.
 */

/**
 * @typedef {Object} InteractionOptions
 * @property {import("ol/interaction").Interaction} interaction - L'interaction OpenLayers à ajouter
 * @property {String} label - Libellé de l'interaction
 * @property {String} [icon] - Icône de l'interaction
 */